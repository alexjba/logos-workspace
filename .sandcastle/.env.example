# Claude Code OAuth token — `claude setup-token` on your host. Never set ANTHROPIC_API_KEY alongside it.
CLAUDE_CODE_OAUTH_TOKEN=
# Fine-grained PAT for the logos-fleet org: all repositories; Contents RW, Pull requests RW,
# Issues RW, Metadata R, Workflows RW.
GH_TOKEN=
